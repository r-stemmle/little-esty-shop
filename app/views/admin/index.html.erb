<%= render partial: 'admin/header'%>

<div class='admin_dashboard_body'>
  <div>
    <h3 class="center">Incomplete Invoices</h3>
    <% @invoices.where_not_successful.each do |invoice| %>
      <p>Invoice #<%= link_to "#{invoice.id}", admin_invoice_path(invoice) %> - <%= invoice.created_at.strftime("%m/%d/%Y %H:%M") %></p>
    <% end %>
  </div>

  <div>
    <h3>Top Customers</h3>

  </div>
</div>